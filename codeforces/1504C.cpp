#include <iostream>
#include <map>
#define f first
#define s second
using namespace std;
typedef pair<int,int> pii;



int main(){
    int t; cin >> t;
    while(t--){
        int n; cin >> n;
        int arr[n]; cin >> arr[i];
        map<pii,pii> dp[n+1];
        pii temp1={0,0}, temp2={-1,-1};
        dp[0][temp1]=temp2;
        for(int i=1; i<=n; i++){
            for(pii temp: dp[])
        }
    }
}